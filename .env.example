# Base URL of the ff-server API (see the sibling ff-server repo). Include the
# /api suffix and no trailing slash. This is the default for local dev, where
# ff-server runs on :3000 and this app on :3001.
NEXT_PUBLIC_API_URL=http://localhost:3000/api
